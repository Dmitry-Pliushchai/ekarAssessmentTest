import Foundation
import UIKit

enum MapConstants {
  enum ImageName {
    static let logo = "logo"
  }
  
  enum AnnotationIdentifier {
    static let carLocation = "carLocation"
  }
  
  enum Dimensions {
    static let annotationFrame = CGRect(x: 0, y: 0, width: 40, height: 40)
  }
}
